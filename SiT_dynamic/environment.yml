name: SiT
channels:
  - pytorch
  - nvidia
dependencies:
  - python >= 3.8
  - pytorch >= 1.13
  - torchvision
  - pytorch-cuda >=11.7
  - pip
  - pip:
    - timm
    - diffusers
    - accelerate
    - torchdiffeq
    - wandb
